# ~/.tmuxinator/contentful.yml

name: contentful
root: ~/contentful/lab/

# Optional tmux socket
# socket_name: foo

# Runs before everything. Use it to start daemons etc.
# pre: sudo /etc/rc.d/mysqld start

# Runs in each window and pane before window/pane specific commands. Useful for setting up interpreter versions.
# pre_window: rbenv shell 2.0.0-p247

# Pass command line options to tmux. Useful for specifying a different tmux.conf.
# tmux_options: -f ~/.tmux.mac.conf

# Change the command to call tmux.  This can be used by derivatives/wrappers like byobu.
# tmux_command: byobu
#
# NOTE: use tmux list-windows to get a layout configuration

windows:
  - daemons: vagrant up && vagrant ssh
  - ui:
      layout: fc1f,169x37,0,0{84x37,0,0,0,84x37,85,0[84x25,85,0,2,84x6,85,26,3,84x4,85,33,4]}
      panes:
        - cd user_interface
        - cd user_interface && gitsh
        - cd user_interface
        - cd user_interface && echo "vagrant ssh && jasstart"
  - user_interface: cd user_interface
  - client:
      layout: 224a,208x54,0,0{104x54,0,0,3,103x54,105,0,16}
      panes:
        - cd client
        - cd client && gitsh
  - marketing:
      layout: f032,208x53,0,0{104x53,0,0,6,103x53,105,0[103x36,105,0,13,103x16,105,37,14]}
      panes:
        - cd marketing
        - cd marketing && gitsh
        - #
  - gatekeeper:
      layout: a24a,208x54,0,0{104x54,0,0,6,103x54,105,0,14}
      panes:
        - cd gatekeeper
        - cd gatekeeper && gitsh
  - contentful: cd ..
  - contentful: cd ..
  - contentful: cd ..
